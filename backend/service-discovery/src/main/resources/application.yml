spring:
  application:
    name: 'service-discovery'
  cloud:
    config:
      uri: 'config-service'
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
server:
  port: ${PORT:8761}
